<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Base.Theme.Synrgychall2" parent="Theme.Material3.DayNight.NoActionBar">
        <!-- Customize your light theme here. -->
        <!-- <item name="colorPrimary">@color/my_light_primary</item> -->
        <item name="colorPrimary">@color/day_primary</item>
        <item name="colorPrimaryVariant">@color/day_secondary</item>
        <item name="android:colorBackground">@color/day_background</item>
        <item name="android:textColor">@color/day_text</item>
    </style>

    <style name="Theme.Synrgychall2" parent="Base.Theme.Synrgychall2" />
</resources>